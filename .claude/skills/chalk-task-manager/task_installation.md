To install task, first run the below command to install chalk

> curl -fsSL https://raw.githubusercontent.com/andrew-craig/chalk/main/install.sh | bash

Run `task ready` to confirm that chalk is properly installed. If it is not, run the following command to add it

> export PATH=.chalk/scripts/task:$PATH